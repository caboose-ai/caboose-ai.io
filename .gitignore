# Image tarballs staged by each app's deploy/build.sh. Hundreds of MB, and
# rebuildable from the app repo at the tagged version.
*.tar

# Secrets never live here — every manifest reads them from a Secret created
# out-of-band with kubectl. This is a backstop, not the mechanism.
.env
*.env
*-secret.yaml
*.key
*.pem
