run:
  timeout: 5m
  issues-exit-code: 1
linters:
  disable-all: true
  enable:
    - errcheck
    - gofmt
    - govet
    - ineffassign
    - misspell
issues:
  exclude-rules:
    - path: _test.go
      linters:
        - errcheck
